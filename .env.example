OPENROUTER_API_KEY=
OPENROUTER_MODEL=
NEXT_PUBLIC_SITE_URL=http://localhost:3000
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Spotify playlist feature (Authorization Code + PKCE, no secret needed).
# Create an app at https://developer.spotify.com/dashboard and add your site
# origins + "/" as redirect URIs (e.g. https://www.newportfolkschedule.com/
# and http://127.0.0.1:3000/ for local dev — Spotify no longer allows
# http://localhost, so develop via http://127.0.0.1:3000).
NEXT_PUBLIC_SPOTIFY_CLIENT_ID=
